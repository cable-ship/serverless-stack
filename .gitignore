# Terraform
.terraform/
.terraform.lock.hcl
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Terraform sensitive vars — never commit real values
terraform.tfvars

# Allow committed per-region var-files for CI (non-sensitive defaults)
!terraform/environments/dev/*/terraform.tfvars

# Lambda build artifacts
*.zip

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.env
venv/
.venv/

# OS
.DS_Store
Thumbs.db

# VS Code
.vscode/
